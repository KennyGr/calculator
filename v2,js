let clearButton = document.querySelector("#clear");
clear.addEventListener('click', () => {
    displayedTotal = "0"
    tempTotal = "0"
    displayedTotalPara.textContent = displayedTotal
});

const displayedTotalPara = document.createElement("p");
displayedTotalPara.textContent = displayedTotal
displayedTotalPara.classList.add("screen")
let screenBorder = document.querySelector(".screenBorder");
screenBorder.appendChild(displayedTotalPara);

let one = document.querySelector("#one");
one.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "1"
        displayedTotalPara.textContent = displayedTotal
    }
});

let two = document.querySelector("#two");
two.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "2"
        displayedTotalPara.textContent = displayedTotal
    }
});


let three = document.querySelector("#three");
three.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "3"
        displayedTotalPara.textContent = displayedTotal
    }
});

let four = document.querySelector("#four");
four.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "4"
        displayedTotalPara.textContent = displayedTotal
    }
});

let five = document.querySelector("#five");
five.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "5"
        displayedTotalPara.textContent = displayedTotal
    }
});

let six = document.querySelector("#six");
six.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "6"
        displayedTotalPara.textContent = displayedTotal
    }
});

let seven = document.querySelector("#seven");
seven.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "7"
        displayedTotalPara.textContent = displayedTotal
    }
});

let eight = document.querySelector("#eight");
eight.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "8"
        displayedTotalPara.textContent = displayedTotal
    }
});

let nine = document.querySelector("#nine");
nine.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "9"
        displayedTotalPara.textContent = displayedTotal
    }
});

let zero = document.querySelector("#zero");
zero.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "";
            }
        displayedTotal += "0"
        displayedTotalPara.textContent = displayedTotal
    }
});

let dot = document.querySelector("#dot");
dot.addEventListener('click', () => {
    if (displayedTotal == "0") {
            displayedTotal = "0.";
    } else if (displayedTotal == ".") {
        displayedTotal = "0."
    } else {
        displayedTotal += ".";
    }
        displayedTotalPara.textContent = displayedTotal
    }
});

let divide = document.querySelector("#divide");
divide.addEventListener('click', division);

let times = document.querySelector("#times");
times.addEventListener('click', multiplication);

let minus = document.querySelector("#minus");
minus.addEventListener('click', subtraction);

let plus = document.querySelector("#plus");
plus.addEventListener('click', addition);

let equals = document.querySelector("#equals");
equals.addEventListener('click', evaluate);







let displayedTotal = 0;

function add(a, b) {
    return a + b;
}

function subtract(a, b) {
    return a - b
}

function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    return a / b;
}

function operate(operator, a, b) {
    operator = lastOperator//put input of button press here, might need to change "a" to some kind of tag attached to the button
    a = firstnumber
    b = secondnumber
    if (operator === "+") {
        displayedTotal = addition(tempTotal, c);
    } else if (operator === "-") {
        displayedTotal = subtraction(tempTotal, c);
    } else if (operator === "*") {
        displayedTotal = multiplication(tempTotal, c);
    } else if (operator === "/") {
        displayedTotal = division(tempTotal, c);
    }
};
